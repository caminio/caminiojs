var nginios = require('nginios');

nginios.router.add('/nginios/auth','v1/auth');

// add your routes here
//
// e.g.:
// nginios.router.add( '/', 'main' );
//
// would require a file in app/controllers/main.js defining a / route
