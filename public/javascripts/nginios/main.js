jQuery( function($){

  /*
  $('.tooltip').qtip({
    style: { classes: 'qtip-tipsy' },
    target: 'mouse',
    position: { adjust: { mouse: true, y: 25, x: 30 } }
  });
  */

  $('.tooltip').tooltipster({
    theme: 'tooltipster-light'
  });

});
